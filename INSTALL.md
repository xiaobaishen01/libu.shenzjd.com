# 安装和运行指南

## 📋 前置要求

- Node.js 18+ (推荐 20+)
- npm 或 yarn 或 pnpm

## 🚀 快速安装

### 1. 安装依赖

```bash
npm install
```

如果遇到网络问题，可以使用淘宝镜像：

```bash
npm config set registry https://registry.npmmirror.com
npm install
```

### 2. 启动开发服务器

```bash
npm run dev
```

### 3. 访问应用

打开浏览器访问：`http://localhost:3000`

## 📦 依赖说明

### 生产依赖
- `next`: 15.1.0 - React 框架
- `react`: 19.0.0 - UI 库
- `crypto-js`: 4.2.0 - 加密库
- `xlsx`: 0.18.5 - Excel 导出

### 开发依赖
- `typescript`: 类型检查
- `tailwindcss`: 样式框架
- `eslint`: 代码检查

## 🔧 常见问题

### 问题 1: 安装依赖失败

**解决方案：**
```bash
# 清除 npm 缓存
npm cache clean --force

# 删除 node_modules 并重新安装
rm -rf node_modules package-lock.json
npm install
```

### 问题 2: 端口被占用

**解决方案：**
```bash
# 指定端口启动
npm run dev -- -p 3001
```

### 问题 3: TypeScript 错误

**解决方案：**
```bash
# 生成类型定义
npx tsc --noEmit
```

### 问题 4: Tailwind 不生效

**解决方案：**
```bash
# 重新构建样式
npm run dev
```

## 🌍 环境变量（可选）

创建 `.env.local` 文件：

```env
# 可选：配置默认 GitHub 用户名
NEXT_PUBLIC_DEFAULT_GITHUB_OWNER=your_username

# 可选：配置默认仓库名
NEXT_PUBLIC_DEFAULT_GITHUB_REPO=gift-list-data
```

## 📱 生产部署

### 构建

```bash
npm run build
```

### 运行生产版本

```bash
npm start
```

### 部署到 Vercel

1. 推送到 GitHub 仓库
2. 在 Vercel 导入仓库
3. 自动构建和部署

### 部署到其他平台

支持任何支持 Next.js 的平台：
- Netlify
- Railway
- Render
- 自托管服务器

## 🔐 GitHub Token 权限说明

如果使用 GitHub 同步，Token 需要以下权限：

- `repo` - 完整仓库访问权限
  - 用于读写 `data/` 目录下的文件

**安全建议：**
- 使用私有仓库
- Token 仅存储在本地浏览器
- 定期轮换 Token

## 📊 性能优化

### 首次加载
- Next.js 自动代码分割
- 图片优化（如有）
- 懒加载

### 数据处理
- 礼金数据按需解密
- 分页加载，避免内存溢出
- 浏览器空闲时批量解密

## 🎯 浏览器兼容性

- ✅ Chrome 90+
- ✅ Edge 90+
- ✅ Firefox 88+
- ✅ Safari 14+
- ❌ IE 11（不支持）

## 📞 技术支持

如有问题，请检查：
1. 浏览器控制台错误
2. 网络连接（GitHub 同步需要）
3. localStorage 是否被禁用
4. Token 是否有足够权限

## 🎓 学习资源

- [Next.js 文档](https://nextjs.org/docs)
- [Tailwind CSS 文档](https://tailwindcss.com/docs)
- [CryptoJS 文档](https://cryptojs.gitbook.io/docs/)
- [SheetJS 文档](https://docs.sheetjs.com/)

---

**祝使用愉快！🎉**
